<div class="watchlist-container">
    <% const categories=[{ key: 'series' , name: '📺 电视剧' }, { key: 'movies' , name: '🎬 电影' }, { key: 'anime' ,
        name: '🎌 动漫' } ]; %>

        <% categories.forEach(function(cat){ %>
            <% if (site.data.bangumi[cat.key]) { %>
                <section class="category-section" id="section-<%= cat.key %>">
                    <div class="category-header">
                        <h2 class="category-title">
                            <%= cat.name %>
                        </h2>
                        <div class="scroll-controls">
                            <button class="ctrl-btn prev" onclick="scrollGrid('<%= cat.key %>', -1)">‹</button>
                            <button class="ctrl-btn next" onclick="scrollGrid('<%= cat.key %>', 1)">›</button>
                        </div>
                    </div>

                    <div class="media-scroller" id="scroller-<%= cat.key %>">
                        <% site.data.bangumi[cat.key].forEach(function(item){ %>
                            <div class="media-card">
                                <div class="media-cover">
                                    <img src="https://images.weserv.nl/?url=<%= item.cover %>" alt="<%= item.title %>"
                                        loading="lazy">
                                    <% if (item.status) { %>
                                        <span class="status-tag">
                                            <%= item.status %>
                                        </span>
                                        <% } %>
                                            <% if (item.recommend) { %>
                                                <div class="recommend-badge">推荐 <%= item.recommend %>⭐</div>
                                                <% } %>
                                </div>
                                <div class="media-info">
                                    <h3 class="media-name">
                                        <%= item.title %>
                                    </h3>
                                    <div class="media-meta">
                                        <span class="media-rating">豆瓣 <%= item.rating %></span>
                                    </div>
                                    <p class="media-desc">
                                        <%= item.desc %>
                                    </p>
                                </div>
                            </div>
                            <% }); %>
                    </div>
                </section>
                <% } %>
                    <% }); %>
</div>

<script>
    /**
     * 左右滑动控制函数
     * @param {string} id 类别ID
     * @param {number} direction 方向 (1 为右, -1 为左)
     */
    function scrollGrid(id, direction) {
        const container = document.getElementById('scroller-' + id);
        const scrollAmount = container.clientWidth * 0.8; // 每次滑动容器宽度的80%
        container.scrollBy({
            left: direction * scrollAmount,
            behavior: 'smooth' // 平滑滚动
        });
    }
</script>

<style>
    .watchlist-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 10px;
        position: relative;
    }

    .category-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin: 2rem 0 1rem;
        padding: 0 5px;
    }

    .category-title {
        margin: 0 !important;
        font-size: 1.5rem;
        border-left: 4px solid #4a90e2;
        padding-left: 12px;
    }

    /* 按钮样式 */
    .scroll-controls {
        display: flex;
        gap: 8px;
    }

    .ctrl-btn {
        background: var(--block, #f0f2f5);
        color: var(--text-main, #333);
        border: 1px solid rgba(0, 0, 0, 0.1);
        width: 32px;
        height: 32px;
        border-radius: 50%;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 20px;
        transition: all 0.2s;
        line-height: 1;
        padding-bottom: 4px;
        /* 视觉微调 */
    }

    .ctrl-btn:hover {
        background: #4a90e2;
        color: #fff;
        border-color: #4a90e2;
    }

    /* 关键：隐藏滚动条但保留滚动功能 */
    .media-scroller {
        display: flex;
        gap: 20px;
        overflow-x: auto;
        padding: 10px 5px 20px;

        /* 隐藏 Firefox 滚动条 */
        scrollbar-width: none;
        /* 隐藏 IE/Edge 滚动条 */
        -ms-overflow-style: none;

        scroll-snap-type: x mandatory;
        -webkit-overflow-scrolling: touch;
    }

    /* 隐藏 Chrome, Safari, Opera 的滚动条 */
    .media-scroller::-webkit-scrollbar {
        display: none;
    }

    .media-scroller::-webkit-scrollbar-track {
        background: transparent;
    }

    .media-scroller::-webkit-scrollbar-thumb {
        background: #ddd;
        border-radius: 10px;
    }

    /* 卡片基础设置 */
    .media-card {
        flex: 0 0 200px;
        scroll-snap-align: start;
        background: var(--card-bg, #fff);
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
        transition: transform 0.3s ease;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        border: 1px solid rgba(0, 0, 0, 0.05);
    }

    .media-card:hover {
        transform: translateY(-5px);
    }

    .media-cover {
        position: relative;
        height: 260px;
        width: 100%;
    }

    .media-cover img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    /* 标签与文字 */
    .status-tag {
        position: absolute;
        top: 10px;
        right: 10px;
        background: rgba(0, 0, 0, 0.6);
        color: #fff;
        padding: 2px 8px;
        font-size: 10px;
        border-radius: 4px;
    }

    .recommend-badge {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        background: linear-gradient(transparent, rgba(0, 0, 0, 0.8));
        color: #ff9f43;
        font-size: 11px;
        font-weight: bold;
        padding: 15px 10px 5px;
    }

    .media-info {
        padding: 12px;
        flex-grow: 1;
    }

    .media-name {
        font-size: 1rem !important;
        margin: 0 0 4px 0 !important;
        font-weight: bold;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .media-rating {
        font-size: 0.75rem;
        color: #7f8c8d;
        background: #f0f0f0;
        padding: 2px 6px;
        border-radius: 4px;
    }

    .media-desc {
        font-size: 0.8rem;
        color: #666;
        line-height: 1.5;
        margin-top: 8px;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    @media (max-width: 768px) {
        .media-card {
            flex: 0 0 160px;
        }

        .media-cover {
            height: 210px;
        }

        .scroll-controls {
            display: none;
        }

        /* 手机端通常靠手势，隐藏按钮 */
    }
</style>